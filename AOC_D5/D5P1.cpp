#include <fstream>
#include <iostream>
#include <vector>
#include <sstream>
using namespace std;

int main(){
  vector<char> organization = {{},{},{},{},{},{},{},{},{}};
  string content;
  ifstream crane("input5.txt");
  int i = 0;
  while(getline(crane,content)){
    /*
Use the i variable to iterate through the first line and add the letter to the respective vector in the 2d vector - organization
      */
  }
  /*
Next use the instructions and from each line detect the numbers and then follow the instructions using the push_back() or pop() function
  */
}